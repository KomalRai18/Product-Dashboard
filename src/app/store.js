/**
 * Redux Store — Optional
 *
 * This project uses Context API (src/context/ProductContext.jsx) for
 * state management.  If you prefer Redux Toolkit, install the packages:
 *
 *   npm install @reduxjs/toolkit react-redux
 *
 * Then replace this file with:
 *
 *   import { configureStore } from '@reduxjs/toolkit';
 *   import productReducer from '../features/product/productSlice';
 *
 *   export const store = configureStore({
 *     reducer: {
 *       products: productReducer,
 *     },
 *   });
 *
 *   export default store;
 *
 * And wrap your app with <Provider store={store}> in main.jsx.
 */

export const store = null; // placeholder — Context API is active
